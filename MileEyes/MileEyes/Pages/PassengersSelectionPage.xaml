<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:MileEyes.ViewModels;assembly=MileEyes"
             x:Class="MileEyes.Pages.PassengersSelectionPage" BackgroundColor="{StaticResource primaryBackgroundColor}">
  <ContentPage.BindingContext>
    <viewModels:PassengersSelectionViewModel/>
  </ContentPage.BindingContext>

  <ContentPage.ToolbarItems>
    <ToolbarItem Text="Done" Command="{Binding SelectCommand}"></ToolbarItem>
  </ContentPage.ToolbarItems>
    <ListView ItemsSource="{Binding Passengers}" SelectedItem="{Binding SelectedPassenger}"
              IsRefreshing="{Binding Refreshing}" BackgroundColor="{StaticResource primaryBackgroundColor}">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Name}" TextColor="White" DetailColor="#55FFFFFF" />
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
</ContentPage>